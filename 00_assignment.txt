Interpreter - An interpreter is a program that reads and executes the code that you write.

Compilation - A process in which the interpreter processes the user's code and then turns it into code that the machine can understand.

Identifier -  A user-defined name of a program element.

Keywords - Reserved words in Python

Reserved Keywords in Python:
and, as, assert, beak, class, continue, def, del, elif, else, except, finally, false, for, from global, if, import, in, is, lambda, nonlocal, None, not, or, pass, raise, return, True, try, with, while, yeild

All escape sequence in the Python language:
\\, \`, \", \a, \b, \f, \n, \N{name}, \r, \t, \uxxxx, \Uxxxxxxxx, \ooo, \xhh
